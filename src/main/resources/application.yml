spring:
  application:
    name: UberProject-LocationService
server:
  port: 3005

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

